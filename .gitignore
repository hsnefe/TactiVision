*.png

# Secrets — never commit real keys. Copy `.env.example` → `.env` locally.
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*$py.class

# macOS
.DS_Store

# Editor local history (e.g. Cursor / VS Code)
.history/

# Large video assets (keep locally; not tracked)
*.mp4

# Downloaded YOLO weights
*.pt

# Local virtual environments
.venv/
.venv_gpu/
.venv_*/
/venv/
/env/

# Cursor IDE local debug ingest (NDJSON session logs)
.cursor/debug*.log

# Test / type / lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# Coverage, fuzz, packaging
.coverage
coverage.xml
*.cover
.hypothesis/
htmlcov/
dist/
build/
*.egg

# JetBrains
.idea/

# OS / editor junk
Thumbs.db
*.swp
*~

# Local scratch video dumps
_tmp_*.mp4

# Local tracking/debug artifacts
*.jsonl

# Local packaging/build artifacts
*.egg-info/
python
src/python
