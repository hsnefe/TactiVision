# Football video analysis (YOLO + OpenCV + NumPy + scikit-learn)
opencv-python>=4.8.0
numpy>=1.24.0
ultralytics>=8.0.0
lap>=0.5.12
scikit-learn>=1.3.0
# PyTorch: install per platform (CPU/CUDA). Ultralytics will pull a compatible torch if omitted.
# pip install torch --index-url https://download.pytorch.org/whl/cpu

# Optional: only needed for `--mode field` (Roboflow pitch keypoint model).
# `roboflow` is used by scripts/download_field_model.py to fetch local weights.
# `inference-sdk` powers the hosted Inference API fallback when local weights
# are missing. Both are safe to omit if you only run the default pipeline.
roboflow>=1.1.0
inference-sdk>=0.9.7
